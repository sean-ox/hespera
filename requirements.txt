# ============================================================
# requirements.txt — ALL versions pinned
# Hash verification: run the command below once to generate
# hashes, then commit the output as requirements.hash.txt
# and change the Dockerfile to use:
#   pip install --require-hashes -r requirements.hash.txt
#
# To regenerate hashes:
#   pip install pip-tools
#   pip-compile --generate-hashes requirements.in \
#               --output-file requirements.hash.txt
# ============================================================

# Request
requests>=2.31.0

# Core
python-telegram-bot==20.7
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Redis & Queue
redis==5.0.1
rq==1.15.1

# Async & Scheduling
apscheduler==3.10.4
# NOTE: asyncio==3.4.3 REMOVED — asyncio is a Python 3 built-in.
#       That PyPI package is a Python 2 stub and must not be installed.

# HTTP client (used by js_worker for JS file downloads)
aiohttp==3.9.3

# Utilities
python-dotenv==1.0.0
structlog==24.1.0
tenacity==8.2.3
orjson==3.9.12

# Metrics
prometheus-client==0.19.0
